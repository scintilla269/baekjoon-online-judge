#include <iostream>
using namespace std;

int main() {
    int N;
    cin >> N;
    for (int i = 0; i < N; ++i) {
        int num;
        cin >> num;
    }
    cout << (N ? "1.00" : "divide by zero");
    return 0;
}
